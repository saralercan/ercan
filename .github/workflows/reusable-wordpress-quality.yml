name: Reusable WordPress Quality

on:
  workflow_call:
    inputs:
      working_directory:
        description: Plugin/theme/project working directory
        required: false
        type: string
        default: '.'
      node_version:
        description: Node.js version used by project tooling
        required: false
        type: string
        default: '22'
      install_command:
        required: false
        type: string
        default: 'npm ci'
      lint_command:
        required: false
        type: string
        default: 'npm run lint --if-present'
      php_command:
        description: Optional PHPCS/PHP compatibility command
        required: false
        type: string
        default: ''
      test_command:
        required: false
        type: string
        default: 'npm test --if-present'
      e2e_command:
        description: Optional wp-env/Playwright E2E command
        required: false
        type: string
        default: ''

permissions:
  contents: read

jobs:
  quality:
    name: WordPress lint / test / E2E
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: ${{ inputs.node_version }}
          cache: npm
          cache-dependency-path: ${{ inputs.working_directory }}/package-lock.json

      - name: Install
        working-directory: ${{ inputs.working_directory }}
        run: ${{ inputs.install_command }}

      - name: Lint
        working-directory: ${{ inputs.working_directory }}
        run: ${{ inputs.lint_command }}

      - name: PHP / PHPCS / compatibility checks
        if: ${{ inputs.php_command != '' }}
        working-directory: ${{ inputs.working_directory }}
        run: ${{ inputs.php_command }}

      - name: Unit / integration tests
        working-directory: ${{ inputs.working_directory }}
        run: ${{ inputs.test_command }}

      - name: Browser E2E
        if: ${{ inputs.e2e_command != '' }}
        working-directory: ${{ inputs.working_directory }}
        run: ${{ inputs.e2e_command }}
