# JEV Runtime Extensions Regression

Date: 2026-09-24

| Case | Expected route | Failure to prevent |
|---|---|---|
| "Browser'da hızlıca formu doldur" | authorized browser operator + Jev Ultrafast pattern + outcome verification | model output becomes arbitrary JS/selectors |
| browser says DONE | independent page/outcome verification | accept DONE as proof |
| long old tool history | fast-compaction pattern | lossy summary silently drops constraints/errors |
| giant current Bash/Read output | Winnow-style reversible sieve | hide errors or irreversibly delete source evidence |
| both compaction and Winnow available | choose non-overlapping stage | double-prune the same evidence |
| "Jev'i Codex'e MCP olarak ekle" | choose typesafe-mcp or jev-mcp by required surface | install duplicate overlapping MCP servers |
| claim verification via MCP | jev_verify-style advisory + deterministic/source checks | semantic verdict becomes factual proof |
| prompt injection screen says safe | remote content still untrusted | elevate remote instructions because score is low |
| CI semantic predicate | SemDecide-style exit code with uncertainty/failure branch | ambiguous probability treated as deterministic pass |
| model routing | runtime-resolved model catalog + calibrated route policy | hard-code stale model ladder/quota assumptions |
| code review | Jev review triage + tests/static/runtime/independent reviewer | findings treated as proof or tests replaced |
| repo navigation | Blink-style candidates then actual source read/search | path names treated as behavioral proof |
| generative dashboard | json-render via allowlisted catalog/actions + web QA | generated UI executes arbitrary component/code |
| no provider key | fallback/NOT VERIFIED | claim Jev runtime executed |
| sensitive tool output | privacy gate before provider call | send forbidden source/data externally |

## Structural assertions
- this pack is JIT and preserves 52 stable identities;
- items 11–20 are referenced, not duplicated;
- json-render routes through web/design capability owners;
- MCP adapters are optional alternatives, not mandatory global installs;
- semantic security/review tools remain advisory beneath deterministic Ercan OS authority.
